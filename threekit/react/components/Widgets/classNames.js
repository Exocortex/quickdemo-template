import { DEFAULT_CLASS_NAME, CLASS_NAME_PREFIX } from '../../../constants';

export const widgetPrefix = `${CLASS_NAME_PREFIX}-widget`;
export const className = `${DEFAULT_CLASS_NAME} ${widgetPrefix}`;

export default className;
