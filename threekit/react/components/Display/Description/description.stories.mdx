import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Description } from './index';

export const Template = (args) => <Description {...args} />;

<Meta title="Display/Description" />

# Description

### Display a Product's Description

This component will display a description for the product initialized
in the Threekit Player. By default the the description data comes
from the metadata on the initialized Catalog Item, under the key
`_description`.

Passing a string into the `description` prop will display that value
instead.

<Canvas>
  <Story
    name="Default"
    args={{
      description: 'This is the description for a single-product configurator.',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Description} />
