import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Title } from './index';

export const Template = (args) => <Title {...args} />;

<Meta title="Display/Title" />

# Title

### Display the Product Title

This component will display the title of the Item used to
initialize the Threekit Player.

Passing in a string into the `title` prop will display that
value instead.

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Product Title',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Title} />
