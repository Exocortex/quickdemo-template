import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { AttributeValue } from './index';

export const Template = (args) => <AttributeValue {...args} />;

<Meta title="Display/Attribute Value" />

# Attribute Value

### Display an Attribute's value

This component will display the value of the attribute
specified in the `attribute` prop.

Passing a string into the `value` prop will display that
value instead.

<Canvas>
  <Story name="Default" args={{ value: 'Blue' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={AttributeValue} />
