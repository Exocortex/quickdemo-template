import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Price } from './index';

export const Template = (args) => <Price {...args} />;

<Meta title="Display/Price" />

# Price

### Display the Configuration's Price

This component will display the total price of the configuration.

Passing a string/number into the `price` prop will display that
value instead.

<Canvas>
  <Story
    name="Default"
    args={{
      price: '$500',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Price} />
