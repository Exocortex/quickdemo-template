import { Meta, Story, Canvas } from '@storybook/addon-docs';

import { Title } from './Title';

<Meta title="Display/Introduction" component={Title} />

# Display Components

Display components can be used to display data sources from the Threekit API, for example the `product title`, `attribute titles`, `price`, etc.
