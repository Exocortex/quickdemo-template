import { DEFAULT_CLASS_NAME, CLASS_NAME_PREFIX } from '../../../constants';

export const className = `${DEFAULT_CLASS_NAME} ${CLASS_NAME_PREFIX}-input`;

export const classPrefix = CLASS_NAME_PREFIX;

export default className;
